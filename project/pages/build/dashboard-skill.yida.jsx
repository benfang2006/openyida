/* Generated by OpenYida compatibility compiler. Edit the .oyd.jsx source when possible. */

var DASHBOARD = {
  title: 'OpenYida Dashboard Skill E2E',
  subtitle: "OY_E2E_20260626033024 · yida-dashboard verification",
  formUuid: "",
  reportId: "",
  generatedAt: "2026-06-26T03:30:29.609Z"
};
var KPIS = [{
  label: '健康指数',
  value: '96',
  unit: '/100',
  trend: '+4.8%'
}, {
  label: '真实资源',
  value: '4',
  unit: '类',
  trend: 'App/Form/Page/Report'
}, {
  label: '发布状态',
  value: 'OK',
  unit: '',
  trend: 'health-check'
}, {
  label: '看板模式',
  value: 'ON',
  unit: '',
  trend: 'isRenderNav=false'
}];
var CHECKS = ['Dashboard mode custom page created', 'KPI control tower rendered', 'Responsive chart grid rendered', 'Screenshot buttons carry sl-no-capture', 'No mouse-enter event handlers used', 'Report and form resource IDs displayed'];
var TIME_FILTERS = ['今日', '本周', '本月', '全链路'];
function captureLabel(style) {
  return <span className="sl-no-capture" style={style}>截图</span>;
}
function kpiCard(item, index) {
  var colors = ['#2563eb', '#16a34a', '#7c3aed', '#d97706'];
  return <div key={item.label} style={{
    background: '#fff',
    border: '1px solid #d9e2ef',
    borderRadius: 8,
    padding: 18,
    minHeight: 126,
    position: 'relative'
  }}>
      {captureLabel({
      position: 'absolute',
      top: 12,
      right: 12,
      height: 28,
      lineHeight: '28px',
      border: '1px solid #d0d7e2',
      borderRadius: 6,
      background: '#fff',
      color: '#344054',
      padding: '0 10px',
      fontSize: 13,
      fontWeight: 800
    })}
      <div style={{
      color: '#64748b',
      fontSize: 13,
      fontWeight: 700
    }}>{item.label}</div>
      <div style={{
      marginTop: 16
    }}><span style={{
        color: colors[index % colors.length],
        fontSize: 34,
        fontWeight: 850
      }}>{item.value}</span><span style={{
        color: '#667085',
        marginLeft: 6
      }}>{item.unit}</span></div>
      <div style={{
      marginTop: 8,
      color: '#16a34a',
      fontSize: 13,
      fontWeight: 800
    }}>{item.trend}</div>
    </div>;
}
function chartPanel(title, rows) {
  return <section style={{
    background: '#fff',
    border: '1px solid #d9e2ef',
    borderRadius: 8,
    padding: 18,
    minHeight: 238
  }}>
      <div style={{
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'center',
      marginBottom: 16
    }}>
        <div style={{
        fontSize: 18,
        fontWeight: 850
      }}>{title}</div>
        {captureLabel({
        display: 'inline-flex',
        alignItems: 'center',
        height: 30,
        border: '1px solid #d0d7e2',
        borderRadius: 6,
        background: '#fff',
        color: '#344054',
        padding: '0 10px',
        fontSize: 13,
        fontWeight: 800
      })}
      </div>
      {rows.map(row => {
      return <div key={row.label} style={{
        marginBottom: 14
      }}>
            <div style={{
          display: 'flex',
          justifyContent: 'space-between',
          fontSize: 13,
          marginBottom: 6
        }}><span>{row.label}</span><span>{row.value}%</span></div>
            <div style={{
          height: 10,
          background: '#edf2f7',
          borderRadius: 999,
          overflow: 'hidden'
        }}><div style={{
            width: row.value + '%',
            height: '100%',
            background: row.color
          }} /></div>
          </div>;
    })}
    </section>;
}
var _customState = {
  activeFilter: '全链路'
};
function resolveCustomState(ctx) {
  var runtimeState = ctx && ctx.state && ctx.state.__openYidaCompatState;
  return Object.assign({}, _customState, runtimeState || {});
}
export function getCustomState(key) {
  var state = resolveCustomState(this);
  if (key) {
    return state[key];
  }
  return state;
}
export function setCustomState(newState) {
  var nextState = Object.assign({}, resolveCustomState(this), newState || {});
  _customState = nextState;
  this.setState({
    __openYidaCompatState: nextState,
    timestamp: new Date().getTime()
  });
}
export function forceUpdate() {
  this.setState({
    timestamp: new Date().getTime()
  });
}
export function didMount() {}
export function didUnmount() {}
export function renderJsx() {
  var activeFilter = this.getCustomState('activeFilter');
  return <div style={{
    minHeight: '100vh',
    background: '#f4f7fb',
    color: '#172033',
    fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif',
    padding: 28,
    boxSizing: 'border-box'
  }}><div style={{
      display: "none"
    }}>{this.state && this.state.timestamp}</div>
        <div style={{
      maxWidth: 1280,
      margin: '0 auto'
    }}>
          <header style={{
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'flex-end',
        gap: 20,
        marginBottom: 18
      }}>
            <div>
              <div style={{
            fontSize: 13,
            color: '#2563eb',
            fontWeight: 850,
            marginBottom: 8
          }}>YIDA DASHBOARD SKILL</div>
              <h1 style={{
            fontSize: 32,
            lineHeight: '40px',
            margin: 0
          }}>{DASHBOARD.title}</h1>
              <div style={{
            fontSize: 14,
            color: '#667085',
            marginTop: 8
          }}>{DASHBOARD.subtitle}</div>
            </div>
            <div style={{
          background: '#dcfce7',
          color: '#166534',
          border: '1px solid #86efac',
          borderRadius: 999,
          padding: '8px 14px',
          fontWeight: 850,
          fontSize: 13
        }}>PASSED</div>
          </header>
          <section style={{
        display: 'flex',
        flexWrap: 'wrap',
        gap: 10,
        background: '#fff',
        border: '1px solid #d9e2ef',
        borderRadius: 8,
        padding: 12,
        marginBottom: 14
      }}>
            {TIME_FILTERS.map(label => {
          return <button key={label} type="button" onClick={() => this.setCustomState({
            activeFilter: label
          })} style={{
            height: 32,
            padding: '0 12px',
            border: label === activeFilter ? '1px solid #2563eb' : '1px solid #d0d7e2',
            borderRadius: 6,
            background: label === activeFilter ? '#eff6ff' : '#fff',
            color: label === activeFilter ? '#1d4ed8' : '#344054',
            fontWeight: 800
          }}>{label}</button>;
        })}
          </section>
          <section style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(auto-fit, minmax(210px, 1fr))',
        gap: 14,
        marginBottom: 14
      }}>{KPIS.map(kpiCard)}</section>
          <section style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(auto-fit, minmax(320px, 1fr))',
        gap: 14,
        marginBottom: 14
      }}>
            {chartPanel('模块覆盖率', [{
          label: 'App/Form/Page',
          value: 100,
          color: '#2563eb'
        }, {
          label: 'Data/Permission/Share',
          value: 100,
          color: '#16a34a'
        }, {
          label: 'Report/Export/Import',
          value: 100,
          color: '#7c3aed'
        }])}
            {chartPanel('真实资源健康度', [{
          label: 'Form schema',
          value: 100,
          color: '#0ea5e9'
        }, {
          label: 'Dashboard publish',
          value: 100,
          color: '#22c55e'
        }, {
          label: 'Report append',
          value: 100,
          color: '#f59e0b'
        }])}
          </section>
          <section style={{
        display: 'grid',
        gridTemplateColumns: 'minmax(0, 1fr) minmax(320px, 420px)',
        gap: 14
      }}>
            <div style={{
          background: '#fff',
          border: '1px solid #d9e2ef',
          borderRadius: 8,
          padding: 18
        }}>
              <div style={{
            fontSize: 18,
            fontWeight: 850,
            marginBottom: 12
          }}>Dashboard Skill Checks</div>
              {CHECKS.map(item => {
            return <div key={item} style={{
              padding: '10px 0',
              borderBottom: '1px solid #edf1f7',
              color: '#344054'
            }}>✓ {item}</div>;
          })}
            </div>
            <div style={{
          background: '#101828',
          borderRadius: 8,
          padding: 18,
          color: '#e5e7eb'
        }}>
              <div style={{
            fontSize: 18,
            fontWeight: 850,
            marginBottom: 12
          }}>Resource Binding</div>
              <div style={{
            fontFamily: 'Menlo, Consolas, monospace',
            fontSize: 12,
            lineHeight: '22px',
            wordBreak: 'break-all'
          }}><div>formUuid: {DASHBOARD.formUuid || 'n/a'}</div><div>reportId: {DASHBOARD.reportId || 'n/a'}</div><div>generatedAt: {DASHBOARD.generatedAt}</div></div>
            </div>
          </section>
        </div>
      </div>;
}