/* Generated by OpenYida compatibility compiler. Edit the .oyd.jsx source when possible. */

var DASHBOARD = {
  title: '全球业务经营驾驶舱',
  subtitle: "OY_E2E_20260626033024 · real business dashboard acceptance",
  appType: "APP_PZCW6QI5KXG5HTHDEK0Z",
  formUuid: "",
  reportId: "",
  generatedAt: "2026-06-26T03:30:32.366Z"
};
var KPIS = [{
  label: '全球营收',
  value: '¥12.8亿',
  trend: '+18.6%',
  tone: '#f97316'
}, {
  label: '履约准时率',
  value: '97.4%',
  trend: '+3.2%',
  tone: '#2563eb'
}, {
  label: '经营健康度',
  value: '92',
  trend: '稳健',
  tone: '#16a34a'
}, {
  label: '风险闭环率',
  value: '88%',
  trend: '+9.1%',
  tone: '#7c3aed'
}];
var CHARTS = [{
  title: '区域营收贡献',
  rows: [['华东', 92], ['华南', 78], ['海外', 64]]
}, {
  title: '渠道增长结构',
  rows: [['直营', 86], ['生态伙伴', 72], ['线上', 68]]
}, {
  title: '客户分层表现',
  rows: [['战略客户', 94], ['成长客户', 75], ['长尾客户', 58]]
}, {
  title: '供应履约水位',
  rows: [['库存健康', 82], ['物流时效', 89], ['异常处理', 71]]
}, {
  title: '利润质量趋势',
  rows: [['毛利率', 76], ['费用率优化', 67], ['现金回款', 84]]
}, {
  title: '组织协同效率',
  rows: [['审批效率', 88], ['待办响应', 79], ['跨区协同', 73]]
}];
var RISKS = [{
  level: '高',
  title: '海外履约延迟',
  owner: '供应链中心',
  action: '发起专项派单'
}, {
  level: '中',
  title: '重点客户续约波动',
  owner: '大客户部',
  action: '跟进续约计划'
}, {
  level: '中',
  title: '华南费用率偏高',
  owner: '财务 BP',
  action: '复盘费用结构'
}];
var BUSINESS_TIME_FILTERS = ['今日', '本周', '本月', 'FY2026'];
var BUSINESS_REGION_FILTERS = ['全球', '华东', '华南', '海外'];
function captureButton() {
  return <span className="sl-no-capture" style={{
    display: 'inline-flex',
    alignItems: 'center',
    height: 28,
    border: '1px solid rgba(255,255,255,0.28)',
    borderRadius: 999,
    background: 'rgba(255,255,255,0.12)',
    color: '#fff',
    padding: '0 12px',
    fontWeight: 800,
    fontSize: 13
  }}>截图</span>;
}
function kpiCard(item) {
  return <section key={item.label} style={{
    position: 'relative',
    borderRadius: 18,
    padding: 20,
    minHeight: 132,
    background: 'linear-gradient(135deg, rgba(15,23,42,0.92), rgba(30,41,59,0.86))',
    border: '1px solid rgba(255,255,255,0.16)',
    boxShadow: '0 18px 50px rgba(15,23,42,0.18)'
  }}>
      <div style={{
      position: 'absolute',
      top: 16,
      right: 16
    }}>{captureButton()}</div>
      <div style={{
      color: '#cbd5e1',
      fontSize: 13,
      fontWeight: 800
    }}>{item.label}</div>
      <div style={{
      marginTop: 18,
      color: item.tone,
      fontSize: 34,
      lineHeight: '40px',
      fontWeight: 900
    }}>{item.value}</div>
      <div style={{
      marginTop: 8,
      color: '#e2e8f0',
      fontSize: 13,
      fontWeight: 800
    }}>{item.trend}</div>
    </section>;
}
function chartPanel(item, index) {
  var palette = ['#f97316', '#2563eb', '#16a34a', '#7c3aed', '#0ea5e9', '#d97706'];
  return <section key={item.title} style={{
    borderRadius: 18,
    padding: 18,
    background: '#ffffff',
    border: '1px solid #eadfd4',
    minHeight: 220,
    boxShadow: '0 14px 36px rgba(121,85,54,0.10)'
  }}>
      <div style={{
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'center',
      marginBottom: 16
    }}>
        <h3 style={{
        margin: 0,
        color: '#1f2937',
        fontSize: 17,
        fontWeight: 900
      }}>{item.title}</h3>
        <span className="sl-no-capture" style={{
        display: 'inline-flex',
        alignItems: 'center',
        height: 28,
        border: '1px solid #fed7aa',
        borderRadius: 999,
        background: '#fff7ed',
        color: '#c2410c',
        padding: '0 10px',
        fontWeight: 800,
        fontSize: 13
      }}>截图</span>
      </div>
      {item.rows.map(row => {
      return <div key={row[0]} style={{
        marginBottom: 14
      }}>
            <div style={{
          display: 'flex',
          justifyContent: 'space-between',
          color: '#475569',
          fontSize: 13,
          marginBottom: 7
        }}><span>{row[0]}</span><strong>{row[1]}%</strong></div>
            <div style={{
          height: 10,
          background: '#f1f5f9',
          borderRadius: 999,
          overflow: 'hidden'
        }}><div style={{
            width: row[1] + '%',
            height: '100%',
            background: palette[index % palette.length],
            borderRadius: 999
          }} /></div>
          </div>;
    })}
    </section>;
}
function riskRow(item) {
  return <div key={item.title} style={{
    display: 'grid',
    gridTemplateColumns: '42px minmax(0, 1fr) 120px 120px',
    gap: 12,
    alignItems: 'center',
    padding: '12px 0',
    borderBottom: '1px solid rgba(255,255,255,0.12)'
  }}>
      <span style={{
      display: 'inline-flex',
      width: 32,
      height: 32,
      borderRadius: 999,
      background: item.level === '高' ? '#ef4444' : '#f59e0b',
      color: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
      fontWeight: 900
    }}>{item.level}</span>
      <strong style={{
      color: '#fff'
    }}>{item.title}</strong>
      <span style={{
      color: '#cbd5e1'
    }}>{item.owner}</span>
      <span className="sl-no-capture" style={{
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      height: 30,
      border: '1px solid rgba(255,255,255,0.26)',
      borderRadius: 999,
      background: 'rgba(255,255,255,0.10)',
      color: '#fff',
      fontWeight: 800
    }}>{item.action}</span>
    </div>;
}
var _customState = {
  activeTime: 'FY2026',
  activeRegion: '全球'
};
function resolveCustomState(ctx) {
  var runtimeState = ctx && ctx.state && ctx.state.__openYidaCompatState;
  return Object.assign({}, _customState, runtimeState || {});
}
export function getCustomState(key) {
  var state = resolveCustomState(this);
  if (key) {
    return state[key];
  }
  return state;
}
export function setCustomState(newState) {
  var nextState = Object.assign({}, resolveCustomState(this), newState || {});
  _customState = nextState;
  this.setState({
    __openYidaCompatState: nextState,
    timestamp: new Date().getTime()
  });
}
export function forceUpdate() {
  this.setState({
    timestamp: new Date().getTime()
  });
}
export function didMount() {}
export function didUnmount() {}
export function renderJsx() {
  var activeTime = this.getCustomState('activeTime');
  var activeRegion = this.getCustomState('activeRegion');
  return <main style={{
    minHeight: '100vh',
    background: 'linear-gradient(135deg, #fff7ed 0%, #eff6ff 42%, #f8fafc 100%)',
    padding: 28,
    boxSizing: 'border-box',
    fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif'
  }}><div style={{
      display: "none"
    }}>{this.state && this.state.timestamp}</div>
        <div style={{
      maxWidth: 1440,
      margin: '0 auto'
    }}>
          <header style={{
        borderRadius: 24,
        padding: 28,
        background: 'linear-gradient(135deg, #0f172a 0%, #1e3a8a 52%, #92400e 100%)',
        color: '#fff',
        marginBottom: 18,
        boxShadow: '0 24px 70px rgba(15,23,42,0.24)'
      }}>
            <div style={{
          display: 'flex',
          justifyContent: 'space-between',
          gap: 24,
          alignItems: 'flex-start'
        }}>
              <div>
                <div style={{
              color: '#fed7aa',
              fontSize: 13,
              fontWeight: 900,
              letterSpacing: 1.8
            }}>BUSINESS DASHBOARD ACCEPTANCE</div>
                <h1 style={{
              margin: '10px 0 8px',
              fontSize: 38,
              lineHeight: '46px',
              fontWeight: 950
            }}>{DASHBOARD.title}</h1>
                <p style={{
              margin: 0,
              color: '#dbeafe',
              fontSize: 15
            }}>{DASHBOARD.subtitle}</p>
              </div>
              <div style={{
            textAlign: 'right',
            color: '#e0f2fe',
            fontSize: 13,
            lineHeight: '22px'
          }}>
                <div>appType: {DASHBOARD.appType || 'n/a'}</div>
                <div>formUuid: {DASHBOARD.formUuid || 'n/a'}</div>
                <div>reportId: {DASHBOARD.reportId || 'n/a'}</div>
              </div>
            </div>
          </header>
  
          <section style={{
        display: 'flex',
        flexWrap: 'wrap',
        gap: 10,
        alignItems: 'center',
        padding: 14,
        borderRadius: 18,
        background: 'rgba(255,255,255,0.84)',
        border: '1px solid #e5e7eb',
        marginBottom: 18
      }}>
            {BUSINESS_TIME_FILTERS.map(label => {
          return <button key={label} type="button" onClick={() => this.setCustomState({
            activeTime: label
          })} style={{
            height: 34,
            border: label === activeTime ? '1px solid #f97316' : '1px solid #cbd5e1',
            borderRadius: 999,
            background: label === activeTime ? '#fff7ed' : '#fff',
            color: label === activeTime ? '#c2410c' : '#334155',
            padding: '0 14px',
            fontWeight: 850
          }}>{label}</button>;
        })}
            <span style={{
          color: '#94a3b8'
        }}>|</span>
            {BUSINESS_REGION_FILTERS.map(label => {
          return <button key={label} type="button" onClick={() => this.setCustomState({
            activeRegion: label
          })} style={{
            height: 34,
            border: '1px solid #dbeafe',
            borderRadius: 999,
            background: label === activeRegion ? '#eff6ff' : '#fff',
            color: '#1d4ed8',
            padding: '0 14px',
            fontWeight: 850
          }}>{label}</button>;
        })}
          </section>
  
          <section style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))',
        gap: 16,
        marginBottom: 18
      }}>{KPIS.map(kpiCard)}</section>
          <section style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(auto-fit, minmax(360px, 1fr))',
        gap: 16,
        marginBottom: 18
      }}>{CHARTS.map(chartPanel)}</section>
  
          <section style={{
        borderRadius: 22,
        padding: 22,
        background: 'linear-gradient(135deg, #111827, #1e293b)',
        color: '#fff',
        border: '1px solid rgba(255,255,255,0.14)'
      }}>
            <div style={{
          display: 'flex',
          justifyContent: 'space-between',
          alignItems: 'center',
          marginBottom: 10
        }}>
              <h2 style={{
            margin: 0,
            fontSize: 22,
            fontWeight: 950
          }}>风险水位与行动闭环</h2>
              {captureButton()}
            </div>
            {RISKS.map(riskRow)}
            <div style={{
          marginTop: 16,
          color: '#cbd5e1',
          fontSize: 13
        }}>页面内派单入口用于验收交互闭环；真实钉钉待办由 saveFormData → 集成自动化 → 待办2.0 连接器链路承接。</div>
          </section>
        </div>
      </main>;
}